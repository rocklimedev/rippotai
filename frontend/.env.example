# Public site URL (used by sitemap/robots and metadata)
NEXT_PUBLIC_SITE_URL=https://rippotaiarchitecture.com

# Admin console basic-auth (middleware). Leave empty to keep /admin open in local dev.
ADMIN_USER=
ADMIN_PASSWORD=

# Where contact / career submissions are delivered (used by /api/* once wired to a mail provider)
CONTACT_TO=sagar@rippotai.in
